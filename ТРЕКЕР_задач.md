# План задач до старта разработки приложения

**Дата создания:** 18 мая 2026
**Назначение:** рабочий трекер. Задачи упорядочены по зависимостям. Решаем по очереди сверху вниз. Блокеры закрываются до начала кодирования ядра; параллельные задачи можно вести одновременно с блокерами.

**Условные обозначения статуса:**
- ⬜ не начато
- 🟡 в работе
- ✅ закрыто
- ⏸️ отложено / ждёт зависимости

**Зоны ответственности:**
- Краснов К. С. — разделы 1–6
- Филин И. А., Аловердян Д. А. — разделы 7–13 (большинство блокеров здесь)

---

## ЧТО УЖЕ ЗАКРЫТО (контекст, не трогаем)

- ✅ 15 решений по функционалу — зафиксированы в журнале решений (раздел «Решения по функционалу 7–13»).
- ✅ `Функциональное_описание_v4.md` — написан и сверен с журналом.
- ✅ Раздел 1 — согласован (4 группы КФА уже были в тексте).
- ✅ Раздел 2 — согласован.
- ✅ Раздел 3 v4 — переписан, сверен, рассогласование с Решением №14 закрыто.
- ✅ Микронутриенты vs замечание научрука — закрыто обоснованием отхода в Решении №1.

---

## БЛОКЕРЫ — без них нельзя начинать код ядра

Порядок строгий: каждая следующая задача опирается на предыдущую.

### Задача Б1. Матмодель (раздел 8) ✅
**Статус:** закрыто 18 мая 2026. **Зона:** Филин/Аловердян. **Зависимости:** нет (первоисточник).
**Результат:** `Математическая_модель_обоснование_v2.txt` (571 строка). Переписано: убраны BMR/Миффлин, пятиуровневая шкала TDEE, бюджет, стоимостная оптимизация, два режима покупок, слагаемые cost/liked в F. Добавлены: нормы из таблиц МР 2.3.1.0253-21 (4 группы КФА 1,4/1,6/1,9/2,2), мини-анкета определения КФА, поправка цели ±15% от нормы МР, механизм закрепления блюд с пересчётом остатка, двухфазный алгоритм (фаза 2 перенацелена на балансировку по КБЖУ — вариант В), контроль соответствия с информированием пользователя, перенос недобора микронутриентов с обоснованием отхода от буквального замечания. Сверено: запрещённых элементов нет, все обязательные присутствуют.

### Задача Б2. Алгоритм / блок-схема (раздел 10) ✅
**Статус:** закрыто 18 мая 2026 (черновик для понимания и разработки). **Зона:** Филин/Аловердян. **Зависимости:** Б1.
**Результат:** `Блок-схема_алгоритма_формирования_плана.svg` (13 блоков: вход → целевые → распределение с закреплением → фильтрация → жадный подбор → балансировка по КБЖУ → недобор → проверка коридора с fallback → список покупок → выбор магазина с вилкой/без). Соответствует матмодели v2.
**Важно:** это смысловой черновик алгоритма для разработки. Для диплома раздел 10 нужно отдельно переписать в ГОСТ-оформлении (текущий раздел 10 диплома устарел — построен вокруг бюджета). Это отдельная задача уровня текста ВКР, не блокер кода. Процедура замены блюда не на основной схеме (запускается пользователем на готовом плане) — описывается отдельно при написании раздела 10.

### Задача Б3. Схема БД (раздел 6) ✅
**Статус:** закрыто 18 мая 2026. **Зона:** Филин/Аловердян. **Зависимости:** Б1, Б2 (готовы).
**Результат:** `Схема_БД_Б3.md`. Три кластера (пользовательский, рецептурно-продуктовый, плановый), все таблицы с полями/ключами/связями. Под матмодель v2: kfa_group I–IV вместо 5 уровней; ENERGY_NORMS (нормы из таблиц МР); ACTIVITY_SURVEY (мини-анкета); MICRONUTRIENT_CARRYOVER (недобор); USER_DISH_EXCLUSIONS; PRODUCT_MICRONUTRIENTS; email_confirmed; 6 типов диет. Убрано: таблицы магазинов/цен, бюджет, пропуск, source='user', лайки/дизлайки. Цены НЕ хранятся (price-service stateless, цены из Edadil по запросу). Есть ER-связи (текстом) и таблица соответствия блокам алгоритма. Сверено: запрещённых сущностей нет, обязательные присутствуют.
**Примечание:** текущий раздел 6 диплома устарел (магазины/бюджет/5 уровней/source=user) — переписывается под эту схему отдельно как задача уровня текста ВКР, не блокер кода. ER-диаграмму для диплома (ГОСТ, англ.+рус., «воронья лапка») команда рисует по этой схеме.

### Задача Б4. Контракт «вход → ядро → выход» ✅
**Статус:** закрыто 18 мая 2026. **Зона:** Филин/Аловердян. **Зависимости:** Б1 (готова), Б3 (частично — каркас из DFD).
**Результат:** `Контракт_API_Б4.md`. Зафиксированы: вход ядра (профиль + закрепления + исключения + недобор), выход ядра (план + агрегированный список + compliance + недобор для переноса), контракт price-service по фактической реализации (`min_total_price`/`max_total_price`/`price_range`, выбор магазина через `selected_shop_uuid`, `pricing_scope`), границы ответственности, точки интеграции. Формат `shopping_list` ядра совпадает с входом price-service `items[]`. Обновлён 18 мая под доработанный price-service.
**Примечание:** контракт опирается на каркас БД из DFD-диаграмм пользователя (D1–D6). Полная схема БД (Б3) ещё нужна, но контракт от неё не блокируется — основные сущности уже определены.

---

## ПАРАЛЛЕЛЬНЫЕ ЗАДАЧИ — можно вести одновременно с блокерами

### Задача П1. Проверка доступа к справочнику Скурихина-Тутельяна ⬜
**Статус:** не начато. **Зависимости:** нет. **Срок:** до наполнения каталога блюд.

Проверить, как технически получать данные с web.ion.ru/food/ (API / парсинг / выгрузка). Если машинный доступ невозможен — выработать план Б (ручная выгрузка подмножества продуктов для контрольного примера). Риск-задача: блокирует наполнение каталога, но не архитектуру.

### Задача П2. Доработка price-service ✅
**Статус:** закрыто 18 мая 2026 (Филин/Аловердян выполнили). **Зона:** Филин/Аловердян.

Реализовано: вилка `min_total_price` / `max_total_price` + объект `price_range`; `total_price` оставлен для обратной совместимости (= min). Режим выбранного магазина через `selected_shop_uuid` (Edadil shopUuid конкретной точки), `pricing_scope` = `nearest_shops_range` | `selected_shop`. Добавлен статус позиции `selected_shop_price_not_found`. Сервис stateless, БД/Docker не использует. Документация: `backend-integration-guide.md`, `backend-response-format.md`. Контракт `Контракт_API_Б4.md` обновлён под фактическую реализацию (выбор магазина — `selected_shop_uuid`, НЕ `retailer_slug`).

### Задача П3. Фиксация технологического стека ⬜
**Статус:** частично известно (price-service на Go). **Зависимости:** нет. **Срок:** до старта кода ядра.

Зафиксировать и записать в раздел 11: что на Go, что на Python, какая СУБД (PostgreSQL?), фронт. Раздел 11 диплома подразумевает Go + Python — закрепить разделение.

### Задача П4. Каталог блюд — план наполнения ⬜
**Статус:** не начато. **Зависимости:** П1 (формат данных Скурихина), Б3 (структура каталога). **Срок:** до контрольного примера (раздел 13).

Решить: кто заводит блюда, сколько блюд нужно для контрольного примера и для работоспособности оптимизатора, откуда берутся рецептуры. Не блокер для старта кода (можно начать с тестовых данных), но блокер для раздела 13 и защиты.

---

## ПОРЯДОК РАБОТЫ (визуально)

```
Б1 Матмодель ──► Б2 Алгоритм ──► Б3 Схема БД ──► Б4 Контракт ──► СТАРТ КОДА ЯДРА
   (раздел 8)     (раздел 10)     (раздел 6)      (граница)

Параллельно в любой момент: П1 (Скурихин), П2 (price-service), П3 (стек)
Зависит от П1+Б3:           П4 (каталог блюд)
```

---

## ТЕКУЩИЙ ШАГ

**ВСЕ 4 БЛОКЕРА ЗАКРЫТЫ:** Б1 (матмодель) ✅, Б2 (блок-схема) ✅, Б3 (схема БД) ✅, Б4 (контракт API) ✅.

**→ Можно начинать разработку ядра.**

Что есть у разработчиков для старта:
- `Математическая_модель_обоснование_v2.txt` — что и как оптимизируется
- `Блок-схема_алгоритма_формирования_плана.svg` — пошаговая логика
- `Схема_БД_Б3.md` — структура базы (таблицы, ключи, связи)
- `Контракт_API_Б4.md` — вход/выход ядра, стык с price-service
- `Функциональное_описание_v4.md` — что приложение делает
- Стек: Go (ядро) + Python (загрузка данных) + PostgreSQL + Redis + React (раздел 11 диплома)

Параллельно с разработкой (не блокеры): П1 (доступ к Скурихину-Тутельяну), П4 (наполнение каталога блюд — нужно к контрольному примеру). П2 (доработка price-service) — ✅ закрыта 18 мая.

ОТДЕЛЬНО, текст ВКР (не блокирует код): переписать разделы 6, 8, 10 диплома под матмодель v2 (сейчас под старую модель — бюджет/Миффлин/магазины/пропуск). Карты расхождений по разделам 6 и 10 уже составлены в этой сессии.

---

*Файл обновляется по мере закрытия задач. Статусы проставлять прямо здесь.*
